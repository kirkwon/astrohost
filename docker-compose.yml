version: "3.8"
services:
  web:
    # Build the SSR image (uses Dockerfile.ssr)
    build:
      context: .
      dockerfile: Dockerfile.ssr
    image: astrohost:ssr
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
